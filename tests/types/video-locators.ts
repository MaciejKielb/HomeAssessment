import { Locator } from '@playwright/test';

/**
 * Locator interface for video component
 * Centralized type definitions for video locators
 */
export interface VideoAssertionsLocators {
  videoElements: Locator;
}
